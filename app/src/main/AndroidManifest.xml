<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.rapidkl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".SplashScreen" />
        <activity
            android:name=".UserSpace"
            android:label="@string/title_activity_user_space"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Ticket"
            android:label="QR Code" />
        <activity
            android:name=".ViewDetails"
            android:label="Balance" />
        <activity
            android:name=".TopupCredit"
            android:label="Reload" />
        <activity
            android:name=".RegisterCard"
            android:label="New Card" />
        <activity
            android:name=".CustomerService"
            android:label="Customer Services" />
        <activity
            android:name=".News"
            android:label="Info Update" />
        <activity android:name=".OfflineMode"></activity>
    </application>

</manifest>